# Arvox Bot

🤖 یک ربات تلگرام با شخصیت شوخ‌طبع و قابلیت‌های هوش مصنوعی (پشتیبانی از متن، ویس، تصویر و پاسخ صوتی).

## نصب

1. نصب پیش‌نیازها:
```
pip install -r requirements.txt
```

2. ساخت فایل `.env`:
```
TELEGRAM_BOT_TOKEN=توکن ربات شما
OPENAI_API_KEY=کلید OpenAI شما
```

3. اجرا:
```
python main.py
```